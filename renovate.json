{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "prHourlyLimit": 6,
  "packageRules": [
    {
      "groupName": "Astro Adapters and Integrations",
      "matchPackagePatterns": ["^@astrojs/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessageTopic": "{{#if (eq (length upgrades) 1)}}Update {{upgrades.0.depName}} to {{upgrades.0.newVersion}}{{else}}Update {{length upgrades}} dependencies in Astro Adapters and Integrations group{{/if}}"
    },
    {
      "groupName": "Radix",
      "matchPackagePatterns": ["^@radix-ui/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessageTopic": "{{#if (eq (length upgrades) 1)}}Update {{upgrades.0.depName}} to {{upgrades.0.newVersion}}{{else}}Update {{length upgrades}} dependencies in Radix group{{/if}}"
    },
    {
      "groupName": "Types",
      "matchPackagePatterns": ["^@types/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessageTopic": "{{#if (eq (length upgrades) 1)}}Update {{upgrades.0.depName}} to {{upgrades.0.newVersion}}{{else}}Update {{length upgrades}} dependencies in Types group{{/if}}"
    }
  ]
}
