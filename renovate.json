{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "prHourlyLimit": 6,
  "packageRules": [
    {
      "groupName": "Astro Adapters and Integrations",
      "matchPackagePatterns": ["^@astrojs/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessage": "{{#if (eq updates.length 1)}}Update {{#each updates}}{{depName}} to {{newVersion}}{{/each}}{{else}}Update {{updates.length}} dependencies in Astro Adapters and Integrations group{{/if}}"
    },
    {
      "groupName": "Radix",
      "matchPackagePatterns": ["^@radix-ui/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessage": "{{#if (eq updates.length 1)}}Update {{#each updates}}{{depName}} to {{newVersion}}{{/each}}{{else}}Update {{updates.length}} dependencies in Radix group{{/if}}"
    },
    {
      "groupName": "Types",
      "matchPackagePatterns": ["^@types/"],
      "matchUpdateTypes": ["minor", "patch"],
      "commitMessage": "{{#if (eq updates.length 1)}}Update {{#each updates}}{{depName}} to {{newVersion}}{{/each}}{{else}}Update {{updates.length}} dependencies in Types group{{/if}}"
    },
    {
      "groupName": "Other",
      "matchPackagePatterns": ["*"],
      "matchUpdateTypes": ["minor", "patch"]
    }
  ]
}
