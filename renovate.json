{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "prHourlyLimit": 6,
  "packageRules": [
    {
      "groupName": "Astro Adapters and Integrations",
      "matchPackagePatterns": ["^@astrojs/"],
      "matchUpdateTypes": ["minor", "patch"],
      "prTitle": "{{#if (eq depCount 1)}}Update {{depName}} to {{newVersion}}{{else}}Update {{depCount}} dependencies in Astro Adapters and Integrations group{{/if}}"
    },
    {
      "groupName": "Radix",
      "matchPackagePatterns": ["^@radix-ui/"],
      "matchUpdateTypes": ["minor", "patch"],
      "prTitle": "{{#if (eq depCount 1)}}Update {{depName}} to {{newVersion}}{{else}}Update {{depCount}} dependencies in Radix group{{/if}}"
    },
    {
      "groupName": "Types",
      "matchPackagePatterns": ["^@types/"],
      "matchUpdateTypes": ["minor", "patch"],
      "prTitle": "{{#if (eq depCount 1)}}Update {{depName}} to {{newVersion}}{{else}}Update {{depCount}} dependencies in Types group{{/if}}"
    },
    {
      "groupName": "Other",
      "matchPackagePatterns": ["*"],
      "matchUpdateTypes": ["minor", "patch"]
    }
  ]
}
